<nav class="navbar navbar-expand-lg navbar-light bg-dark bg-gradient px-5">
  <%= link_to spaceships_path, class: "btn btn-outline-light" do %>
    <i class="fa-solid fa-rotate-left"></i> Return
  <% end %>
</nav>

<div class="container mt-5 py-5 bg-light border rounded shadow-sm">
  <div class="row justify-content-center">
    <div class="col-md-8">
        <h2><strong>Post your spaceship </strong>🚀</h2>
        <hr class="mb-4">
          <%= simple_form_for @spaceship do |f| %>
            <%= f.input :name %>
            <%= f.input :location %>
            <div class="d-flex gap-3">
              <%= f.input :cost %>
            </div>
            <%= f.button :submit, class: "btn btn-primary" %>
          <% end %>
    </div>
  </div>
</div>
